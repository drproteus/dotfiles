[user]
	name = Jake Goritski
	email = jgoritski@gmail.com
[color]
    status = auto
    diff = auto
    branch = auto
    decorate = auto
    grep = auto
    interactive = auto
    ui = auto
    showbranch = auto
    pager = true
[push]
    default = simple
[core]
    editor = nvim
	excludesfiles = ~/.gitignore
	excludesfile = ~/.gitignore
[merge]
    ff = no
    editor = nvim
	excludesfiles = ~/.gitignore
	excludesfile = ~/.gitignore
[alias]
	today = !git log --since=midnight --author=\"$(git config user.name)\" --oneline
[branch]
	autosetuprebase = never
